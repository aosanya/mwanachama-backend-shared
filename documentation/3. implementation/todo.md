# mwanachama-backend-shared (Go)

Open tasks only — 🚀 In Progress · 📋 Not Started · ⏸️ Blocked.
Everything else (completed rows, board context) is in [todo_done.md](todo_done.md).

| Task | Title | Status | Depends on |
|------|-------|--------|------------|
| S23 | 🐞 **BUG — `spec.Migrate` builds DDL straight from a `*Spec`'s names and never calls `Validate` itself, so a spec obtained any way other than `Load`/`Parse` gets no protection against an identifier that is not an identifier.** `spec/migrate.go:Migrate` calls `s.DDL(dialect)` and executes each statement; neither `Migrate` nor `Spec.DDL` calls `s.Validate()`. Repro (`spec/s23_migrate_skips_validation_test.go`): a hand-built `*Spec` whose one field is named `` `a text default 'x', injected` `` fails `s.Validate()` as expected, but `spec.Migrate(db, s)` runs anyway and executes `CREATE TABLE i_m_widgets (id text not null, a text default 'x', injected text default '', primary key (id))` — a column nobody declared, minted purely from one field's `Name`. `NamePattern`/`DocPathPattern` are documented as the *only* line of defense against identifiers reaching SQL as raw text ("Never relax this to escape it instead" — `spec/spec.go`'s package doc), but that defense lives solely in `Validate`, which only `Load`/`Parse` call; the exported `Migrate(db *gorm.DB, s *Spec)` takes a bare, mutable `*Spec` with no way to prove it was ever validated. | — |
| S24 | 🐞 **BUG — `spec.Validate`'s physical-name-collision check is scoped to one spec's own object list, so two independently-loaded, independently-valid specs that happen to agree on `instance`+`module`+`table` are never compared to each other and silently share one table.** Repro (`dispatch/s24_cross_spec_table_collision_test.go`): two unrelated blueprints/domains (`widgetworks`/widget, `gadgetworks`/gadget) each declare `instance: "acme"`, `module: "widgetworks"`, and object table `"widgets"` — both pass `spec.Validate()` on their own, and `spec.Migrate` is run for each with no error. A widget created through the first domain's manager (`code: "W-1"`) is then read back by the *second* domain's own `List` call, over its own unrelated `gadget` object — the two domains' rows are the same rows. `declared-domains.md` says collision protection is why "`Validate` refuses it rather than leaving it to the database", but that promise holds only within one `Spec.Validate()` call; nothing in `spec`/`specstore` ever checks a table name Migrate is about to create against one another spec has already claimed in the same database. | — |
