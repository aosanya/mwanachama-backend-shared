# mwanachama-backend-shared — completed tasks

| Task | Title | Completed | Notes |
|------|-------|-----------|-------|
| S22 | `dispatch`: a tool may keep the name its callers already use | 2026-09-24 | Found by `mwanachama-backend-agency`'s AG41. A tool's name is derived from its action (`agency.goal.create` → `agency_goal_create`), which is right for a module whose tools are new and wrong for one converting a surface people already call: agency published `agency_create_goal`, and `mwanachama-wakala-studio` hardcodes that name in three source files and a test. Renaming 57 tools to satisfy a derivation rule would have been a breaking change made for the convenience of the generator, so an operation may now declare `"tool": "agency_create_goal"` and the derivation stays the default. Two operations publishing one tool name is refused at load, the same way two claiming one action or one address already were. |
| S21 | `dispatch`: an address segment a call does not take is declared, not unbound | 2026-09-24 | Found by `mwanachama-backend-agency`'s AG40, converting a route table with nested addresses — `DELETE /values/{valueID}/attachments/{attachmentID}`, where the call takes only the attachment. The dispatcher refused every path wildcard no argument bound, which is the right rule (an unbound wildcard is usually a call quietly dropping something the address said mattered), so the rule stands and the spec gained a way to say so on purpose: `{"from": "path", "as": "valueID", "ignored": true}`. An ignored argument binds nothing, takes no position in the call and becomes no tool parameter; it may only come from the path, must name a wildcard the path actually declares, and may not also be whole, repeated or an overwrite — four refusals, each tested. Catalog needed none of this: it has no nested addresses. |
| S20 | [2. design/declared-domains.md](../2.%20design/declared-domains.md) — the reference for the declared-domain format | 2026-09-24 | AGD-007 decision #4: the format is an org-wide standard from here on, so its reference lives with the engines rather than in the repo that invented it. Covers the two declarations and who owns each, what a field may say (including the two refusals that surprise people — a description is required, and a required field may not carry a default), indexes over columns and over document paths, why identifiers are validated against a strict alphabet rather than escaped, the three-segment physical name and the four ways `Validate` refuses a collision the database would keep quiet about, what `Migrate` does and does not do, the store's name-not-json-tag join and its write-every-column rule, what stays in Go, **the order a repo is converted in**, and what the shape still does not solve (sequences, filters, pagination, per-instance provisioning). Deliberately not a second copy of `developer/documentation/2. design/architecture-spec-driven-modules.md`, which is the org-wide *strategy* and stays where it is: this page is the reference for the packages, and links to it. `dispatcher.md` had a line saying the store half "lives in each module's own `spec` package", which stopped being true with S18 — corrected in the same pass, and the design README now indexes both. |
| S19 | `specstore/` — the generic spec-driven store, so a module with declared objects writes no row structs | 2026-09-24 | Second half of AGD-007's lift. `catalog/store.go` becomes `specstore`: `New(db, spec, carriers)` resolves which table plays which role and refuses at construction when a declared column has no field to hold it or a carried field is undeclared; `Encode`/`Decode` join a declared field to a Go field **by name** through `ColumnName` (`SubmittedBy` → `submitted_by`), never by json tag — `ShareLink.KeyHash` is `json:"-"` and a tag-reading codec would quietly stop storing it, so every share key would fail to match. `Query`/`Take`/`Insert`/`List[T]` came along too: they were the manager's four-line wrappers in catalog and are the same four lines every converted repo would otherwise rewrite. Catalog now keeps a 40-line adapter — its nine role constants and five one-line delegations — so none of its other files changed beyond `st.object` → `st.Object`. **Catalog's own store tests stayed catalog's**, because they are about its types (a ShareLink's hidden hash, an Entry's clearable `approved_at`); shared got 8 tests of its own over an inline `ticket` spec, including the round trip through a real SQLite table, which is what proves the codec and the declared DDL agree about a bool that comes back as `int64` and text that comes back as `[]byte`. **Two things agency needs are deliberately not here**: the `Code` sequence (`"G-1"` per entity type) and the scope/`LIKE`/exact-match filter helpers every `List` builds on. Both are still agency-shaped rather than proven general, and the counter has a live race (agency's AG35) that should be fixed where it is rewritten, not lifted as-is; AG39 decides whether either comes back here. Verified: `gofmt`, `go vet`, `go test ./...` clean in shared and catalog, `go test ./...` clean in `mwanachama-wakala-api`. |
| S18 | `spec/` — the declared-domain loader, moved here from `mwanachama-backend-catalog` | 2026-09-24 | First half of the lift `mwanachama-backend-agency`'s AGD-007 asked for: a module's blueprint, a domain's spec, the `<instance>_<module>_<table>` naming, the strict identifier alphabet, the 63-byte Postgres budget and `Migrate`'s DDL now live here, with catalog importing them. **The tests split rather than moved**, which is what made this more than a file copy: catalog's `spec/spec_test.go` drove half its cases off `../catalog.blueprint.json` and `spec/examples/*.json`, so those cases are catalog's — about its shipped artifacts — not the engine's. The engine's copy runs on fixtures of its own in `spec/testdata/` (a two-role `record` blueprint filled by a clinic and a garage), so nothing in shared reaches into a consumer, and the two-domains-one-database property is proved twice over: `spec.TestTwoDomainsCoexist` on the fixtures, and catalog's new `examples_test.go` (8 tests, all passing) on the specs it actually ships. `spec/examples/` **stays where it is** — `mwanachama-deployment/wakala-api/Dockerfile` copies `agency.catalog.json` into the image by that path, so it is production config, not a test file (catalog's own CAT6 row already says so). Catalog's `domain_agnostic_test.go` scanned `spec/`, which now holds no Go; it scans `routes/` instead, and passes. Verified: `go build`, `go vet`, `gofmt -l .` and `go test ./...` clean in shared and catalog, and `go test ./...` clean in `mwanachama-wakala-api`, whose one `spec` import was a test's. The `matches` pattern registry stays per module (`catalog/patterns.go`) — the spec names a pattern, the module says what the name means — which is what keeps `slug` and `json_object` from becoming shared's vocabulary. |
| S17 | `dispatch.Tools` — the declared operations become MCP tools, with no MCP dependency | 2026-09-23 | Built for DSN-1724: the `mine-agencies` routine is to file what it mines through `mwanachama-wakala-api`'s MCP, and the module it would write to (`mwanachama-backend-catalog`) has no hand-written handler left to hang a tool on and may not carry a domain's words anyway. So the tools come from the same `*.operations.json` the routes do. **No SDK dependency here:** a `Tool` carries `InputSchema` as `json.RawMessage` and `Invoke` as `func(ctx, json.RawMessage) (any, error)`, so `go.mod` is untouched and the ~20-line adapter onto `modelcontextprotocol/go-sdk` lives in the process that already imports it. **One flat object of arguments:** a named argument is its own property; a `whole` body or query is spread one property per field (by `json` tag, or by `query` key for a query struct) and reassembled before the call; an `into` argument replaces the field it overwrites, so `slug` appears exactly once and the address-outranks-the-body rule holds by construction rather than by ordering. An undeclared argument is refused rather than dropped — the tool-side `DisallowUnknownFields`. **Prose is declared, never inferred:** an operation with no `description` fails the whole spec at build (a tool a model cannot read is one it will call wrongly, and startup is the moment to notice), and per-argument prose, required-ness, enum values and read-only-ness come from `Deps.Fields`, keyed `Type.field`, which a module that declares its objects already has. `"field": "Entry.visibility"` lets a scalar argument borrow a field's prose and its permitted values. A read-only field never becomes an argument, which is what stops a tool offering a model a value the manager overwrites. **Errors are the HTTP surface's answers:** a mapped sentinel reaches the caller with its own text, an unmapped one is `internal error`, through the same `httpwire.StatusFor` table — the two surfaces cannot disagree about what a caller is told. Spec additions are all optional (`title`, `description`, and per-argument `description`/`required`/`field`), so every existing spec still parses. 19 tests in `dispatch/tools_test.go` cover the spread, the overwrite, the enum, the required set, `additionalProperties: false`, the unknown-argument and missing-argument refusals, both error paths, and the two build-time refusals. `gofmt`, `go vet`, `go test ./...` clean. Documented in [2. design/dispatcher.md](../2.%20design/dispatcher.md). |
| S1 | Bootstrap repo: `git init`, `go.mod`, `.gitignore`, `Makefile`, four-phase `documentation/` skeleton, `todo.md`/`todo_done.md` | 2026-09-02 | New repo, created alongside [mwanachama-backend-git](../../../mwanachama-backend-git) and [mwanachama-backend-taskmanager](../../../mwanachama-backend-taskmanager) as the shared Postgres entity-graph engine (avoids duplicating that engine in both consumers). |
| S2 | Define storage-agnostic entity-graph contract: `Entity`, `Relationship`, `DataManager`, `SchemaManager`, `Schema`/`TypeDefinition`/`RelationshipDefinition`/`PropertyDefinition` | 2026-09-02 | Ported from `CodeValdSharedLib/entitygraph` + `types` into two packages: [`schema/`](../../../schema) (pure type-system structs, renamed from the original's generic `types` package name) and [`entitygraph/`](../../../entitygraph) (`DataManager`/`SchemaManager` interfaces, `Entity`/`Relationship`/request/filter/result types, sentinel errors, `FindTypeDef`/`FindRelationshipDef`/`ValidateCreateRelationship`/`ValidateSchema`, and the `StringProp`/`BoolProp`/`Int64Prop`/`Float64Prop` converters). Interfaces are otherwise unchanged from the original — this is the seam `mwanachama-backend-git`/`mwanachama-backend-taskmanager` port their business logic against. Dropped along the way: `PathSegment`/`EntityIDParam`/`Code`/`RefCode`/`PublishEvents` fields and the route/topic-generation machinery they supported (`schemaroutes`, `TopicsFromSchema`) — the gateway registers routes and events by hand, it doesn't derive them from a schema; ported `entitygraph_test.go` accordingly (dropped the PathSegment-uniqueness cases, added two new ones for the `UniqueKey`-must-reference-a-declared-property rule, which the original tested only via doc comment). `go build ./...`, `go vet ./...`, `go test ./...` all clean. |
| S7 | Local `Publisher` interface (`Publish(ctx, topic string, payload any) error`) + a trivial in-process implementation callers can use as a default | 2026-09-02 | New [`events/`](../../../events) package (not folded into `entitygraph/` — publishing isn't a graph-store concern). `Publisher` interface + `PublisherFunc` adapter + `NewLogPublisher` (writes to the standard `log` package, mirrors the original SharedLib's `eventbus.LogPublisher`). Pulled forward out of dependency order (S1 done, S3–S6 not) because `mwanachama-backend-taskmanager`'s W2 needed a `Publisher` type to alias as `task.go`'s `CrossPublisher` replacement — real fan-out (webhooks, a queue, in-process subscribers) is still whatever the caller wires in via its own `Publisher` implementation. `go build ./events/...`, `go vet ./events/...` clean. |
| S3 | Postgres schema for `entities`/`relationships`/schema draft+published — **redesigned mid-flight**: not a fixed migration owned by this repo. [`postgres/tablenames.go`](../../postgres/tablenames.go) + [`postgres/ddl.go`](../../postgres/ddl.go) generate the `CREATE TABLE`/`DROP TABLE` SQL parameterized by a `TableNames` struct (`DefaultTableNames("git_")` → `git_entities`/`git_relationships`/`git_schemas_draft`/`git_schemas_published`), mirroring how CodeValdGit/CodeValdWork each fixed their own ArangoDB collection names over the same shared `entitygraph` backend. Each consumer (`mwanachama-backend-git`, `mwanachama-backend-taskmanager`) embeds `postgres.DDL(...)`/`DropDDL(...)` into its **own** golang-migrate migration and owns its own physical tables — go-shared ships the schema shape, not a table a consumer would have to share. | 2026-09-02 | — |
| S4 | Postgres `DataManager`: entity CRUD/upsert/list, relationship CRUD/list, `TraverseGraph` via recursive CTE | 2026-09-02 | [`postgres/entities.go`](../../postgres/entities.go), [`relationships.go`](../../postgres/relationships.go), [`traverse.go`](../../postgres/traverse.go), [`scan.go`](../../postgres/scan.go), [`errors.go`](../../postgres/errors.go) (SQLSTATE → `entitygraph` sentinel error mapping), [`backend.go`](../../postgres/backend.go) (the `Backend` struct, implementing both `DataManager` and `SchemaManager` over one `*sql.DB` + `TableNames`, same shape as the original ArangoDB `Backend`). `UpsertEntity` computes a `unique_key` from the active schema's `TypeDefinition.UniqueKey` and lets Postgres `ON CONFLICT` do the atomic find-or-merge (`entities_agency_unique_key_idx`), rather than a check-then-write race. **Known limitation**, documented in `traverse.go`: the recursive-CTE walk has no visited-vertex guard, so a cyclic relationship graph re-expands every cycle up to `Depth` — acceptable today since neither consumer's schema is cyclic (commit ancestry, task dependencies are DAGs), but worth revisiting if that changes. Required-relationship enforcement (`ErrRequiredRelationshipViolation`) is **not** implemented in `CreateEntity` — treated as a caller concern via the exported `FindTypeDef`/`ValidateCreateRelationship` helpers, consistent with how CodeValdGit/CodeValdWork call those themselves rather than relying on the DataManager to know the schema. |
| S5 | Postgres `SchemaManager`: draft/publish/activate/versions | 2026-09-02 | [`postgres/schema_manager.go`](../../postgres/schema_manager.go). `Activate` is a real transaction (deactivate-then-activate inside one `tx`, backstopped by the partial unique index `..._schemas_published_one_active_idx`) — fixing the "never actually atomic" gap noted in [CLAUDE.md](../../CLAUDE.md) against the original two-sequential-AQL-statements Arango behavior. |
| S6 | Postgres connection/pool helper | 2026-09-02 | [`postgres/conn.go`](../../postgres/conn.go) — ported near-verbatim from `mwanachama-backend-api-gateway/internal/store/postgres/conn.go` (`database/sql` + pgx stdlib driver, not a bare `pgxpool.Pool`, matching the gateway's own convention so this package composes cleanly when it's wired in later). |
| S8 (partial) | In-memory `DataManager`/`SchemaManager` + a shared conformance suite | 2026-09-02 | Added beyond the original plan, at user request ("use memory like mwanachama-backend-api-gateway"): new [`memory/`](../../memory) package (plain Go maps + one mutex — a test/dev double, not a production store) and [`entitygraphtest/`](../../entitygraphtest) (one `Run(t, dm, sm, agencyID)` conformance suite covering schema lifecycle, entity CRUD, upsert-by-unique-key, relationships, and traversal — both `memory` and `postgres` call it instead of keeping two hand-written test suites that could drift apart). `go test ./...` is green today for `entitygraph`, `memory`, and `postgres` — but `postgres`'s conformance test only *runs* against a live database when `POSTGRES_URL` is set (skips otherwise, mirroring the gateway's `make test` vs `make pg && make test-pg` split); it has **not** been executed against a real Postgres yet this session (a docker-based attempt was declined in favor of the memory-first approach) — worth doing before S9 tags a version anyone will actually depend on. |
| S10 | Audit GORM-era duplication across every `mwanachama-backend-*` repo and document what belongs in this repo | 2026-09-06 | Read-only audit, no code moved yet. Five repos (`actor`, `assetmanager`, `git`, `comm`, `forms`) independently migrated off `entitygraph` onto GORM between 2026-09-04 and 09-06, and — starting from `actor`'s shape — each rebuilt the same JSON wire helpers, nullable-string converters, sqlite/Postgres test harnesses, and `TableNames`/`Migrate` wrapper (several repos' own comments say "copied verbatim from actor"). Three still-entitygraph repos (`taskmanager`, `accounting`, `digitaltwin`) separately triplicated a closed-vocabulary/state-machine idiom that's storage-agnostic and applies org-wide. Full findings, evidence per repo, and a proposed `httpwire/`/`gormutil/`/`gormtest/`/`vocab/` package layout: [`2. design/gorm-shared-infrastructure.md`](../2.%20design/gorm-shared-infrastructure.md). Follow-up extraction work filed as S11–S16 on `todo.md`. |
| S12 | Extract `gormtest/`: `OpenSQLiteDB(t)`, `OpenPostgresDB(t, prefix)` wrapping `postgres.Open` | 2026-09-14 | New [`gormtest/`](../../gormtest) package. `OpenSQLiteDB(t) *gorm.DB` opens an in-memory sqlite connection; `OpenPostgresDB(t, prefix) (*gorm.DB, func())` skips the test when `POSTGRES_URL` is unset, otherwise opens it via this repo's existing `postgres.Open` and wraps it in `gorm.io/driver/postgres` — `prefix` is used only to identify the caller in a skip/fatal message, since migration and table naming stay the caller's own (each repo's `TableNames`/`Migrate` shape still differs, per S10's audit). No consumer repo (`actor`/`assetmanager`/`comm`/`forms`/`git`) was touched — swapping each repo's local `testdb_test.go`/`postgres_integration_test.go` copy for this import is `assetmanager`'s S16 first-adopter pass, not this row. `go build ./... && go vet ./... && go test ./...` green. |
| S13 | Extract `httpwire/`: `WriteJSON`/`WriteErr`/`Route`/`Routes`/`StatusFor` | 2026-09-14 | New [`httpwire/`](../../httpwire) package: `WriteJSON`/`WriteErr`/`ReadJSON`/`ReadJSONFields`/`Bearer` (ported byte-for-byte from the gateway's `internal/api/http/wire.go`, the origin every GORM repo's `routes/wire.go` copied), plus `Route{Method,Path,Handler}`/`.Pattern(prefix)` (identical across actor/assetmanager/comm/forms/git's own `routes/routes.go`) and a new generalized `StatusFor(err error, table map[error]int, fallback int) int` per S10's proposed signature — each repo keeps its own sentinel-error table (those are genuinely domain-specific), sharing only the `errors.Is` lookup-and-fallback. Each repo's own `Routes(...)` aggregator function stays put (domain-specific, calls domain constructors) — only the shared `Route`/`Pattern` shape moved. No consumer repo touched; adoption is opportunistic per S10's sequencing note. `go build ./... && go vet ./... && go test ./...` green. |
| S15 | Extract `vocab/`: generic closed-membership set + transition-table helper | 2026-09-14 | New [`vocab/`](../../vocab) package: `Set[T comparable]`/`NewSet`/`Contains` (generalizes accounting's `accountTypes map[AccountType]bool`/`IsAccountType`) and `Transitions[T comparable]`/`NewTransitions`/`CanTransitionTo` (generalizes taskmanager's `TaskStatus.CanTransitionTo`/`WorkflowRunStatus.CanTransitionTo` switch statements and digitaltwin's `AssetStatus.CanTransitionTo` equivalent — confirmed directly against all three repos' own source, not just the design doc's summary — into one declarative state→allowed-next-states table; a state absent from the table permits no transitions, matching every existing repo's terminal-state default). Set and Transitions are independent: a type with a closed vocabulary but no lifecycle uses only Set, a type with real transitions uses only Transitions. Storage-agnostic, zero dependencies — applies to `taskmanager`/`accounting`/`digitaltwin` (still entitygraph) exactly as well as the GORM five. No consumer repo touched; adoption is opportunistic, not part of this row. `go build ./... && go vet ./... && go test ./...` green. |
| S9 | Tag `v0.1.0` once `mwanachama-backend-git`/`mwanachama-backend-taskmanager` are ready to consume it | 2026-09-15 | Tagged `v0.1.0` at `44eaf29` (tip of `master`, matching `origin/master`, working tree otherwise clean apart from this pass's own `todo.md`/`todo_done.md` edits) and pushed to `origin`. Both named consumers ([mwanachama-backend-git](../../../mwanachama-backend-git) and [mwanachama-backend-taskmanager](../../../mwanachama-backend-taskmanager)) already build against this repo via a `replace github.com/aosanya/mwanachama-backend-shared => ../mwanachama-backend-shared` directive in their own `go.mod`, so the tag doesn't change how either actually consumes the code today — it's a release marker for anyone building against a real published version later. Did **not** run S8's own Postgres conformance test against a real database before tagging (that row's note flagged it as "worth doing before this tag") — spinning up a real Postgres container for verification is against this family's standing convention (see `[[feedback_use_memory_backend_for_tests]]`); treating the gap as an accepted, already-documented limitation (S8's own todo_done.md entry) rather than a new blocker. Verified with `go build ./... && go vet ./... && go test ./...`, all green — six files (`memory/backend.go`, `orgpolicy/doc.go`, `orgpolicy/gormstore/tables.go`, `orgpolicy/postgres_integration_test.go`, `orgpolicy/store_impl.go`, `orgsettings/models/property.go`) fail `gofmt -l .`, pre-existing and unrelated to this task, not touched. |
| S16 | First adopter pass: swap `mwanachama-backend-assetmanager`'s local copies for `gormtest`/`httpwire` imports, to validate the extraction before larger repos adopt it | 2026-09-15 | Done in [mwanachama-backend-assetmanager](../../../mwanachama-backend-assetmanager). `testdb_test.go`'s hand-rolled `gorm.Open(sqlite.Open(":memory:"))` → `gormtest.OpenSQLiteDB(t)`; `postgres_integration_test.go`'s manual `POSTGRES_URL`/`postgres.Open`/`gorm.Open` dance → `gormtest.OpenPostgresDB(t, "assetit")` (kept that repo's own table-drop cleanup, since migration/table naming stays the caller's). `routes/wire.go` deleted outright — its `writeJSON`/`writeErr`/`readJSON` were byte-for-byte copies of `httpwire.WriteJSON`/`WriteErr`/`ReadJSON`, so every call site in `location.go`/`asset.go`/`movement.go`/`hold.go` now calls `httpwire` directly. `routes/routes.go`'s local `Route`/`Route.Pattern` replaced by `type Route = httpwire.Route` (a type alias, so this package's own exported `LocationRoutes`/`AssetRoutes`/`MovementRoutes`/`HoldRoutes`/`Routes` signatures didn't need to change). Went one step further than the row's literal wording: the four hand-rolled `errors.Is` switch statements (`locationStatusFor`/`assetStatusFor`/`movementStatusFor`/`holdStatusFor`) were converted to `map[error]int` sentinel tables passed through the new `httpwire.StatusFor` — the one genuinely new (not just ported) piece S13 added, and this row's own point was to validate the extraction before larger repos adopt it, so leaving `StatusFor` unexercised would have missed the actual risk. Confirmed with the user before doing this, since it's a code-shape change, not just an import swap. `doc.go` updated to describe the alias/`httpwire` calls instead of the deleted locals. `gofmt -l .` clean, `go build ./... && go vet ./... && go test ./...` all green (including the pre-existing `routes/routes_test.go` coverage of `Route`/`Pattern`/each `XRoutes` builder, unaffected by the alias). `postgres_integration_test.go` itself still skips without `POSTGRES_URL` — not exercised against real Postgres this pass, same as before. |
| S11 | Decide nullable-pointer vs. empty-string-sentinel as the org convention (actor/assetmanager/comm/git split 3-vs-1, forms avoids both) | 2026-09-15 | **The row's own framing was stale** — checked before deciding, per the user's standing instruction on this backlog. S10's audit characterized this as "actor/assetmanager/comm/git have `StringToNullable`/`nullableToString`, forms has neither," which conflated *helper presence* with *domain convention*. Read every actual call site instead of trusting that summary: `actor.models.Group.ParentID`, `assetmanager.models.Location.ParentLocationID`, `comm.models.{Chat,DirectMessage}.ThreadID`, and `git.models.{Branch,Commit,MergeRequest,Tag}.RepositoryID` are every one of them a plain `string`, `""` = unset — identical to `forms`' convention, zero exceptions. `StringToNullable`/`nullableToString`/`NullableToString` never leak past the row layer — each call lives inside a `*ToRow`/`*FromRow` conversion (or git's root-package `_impl.go` files, which build the row directly), converting exactly one indexed, self-referencing-or-FK-shaped column per repo to real SQL `NULL` (needed for a partial unique index / "root has no parent" case an empty string can't express at the SQL layer) — never a domain-level field type. `forms` has neither helper not because it chose a different convention, but because it has no field of that specific shape yet. **Decision: there was no real split to resolve.** Org-wide domain convention — a domain Go type's optional string field is always plain `string`, `""` = unset — is already unanimous across all five repos and stays that way; nothing changes in any consumer repo (decision-only row, no code touched). `gormutil`'s extraction (S14, now unblocked) should package `StringToNullable`/`NullableToString` (exported — comm/git's spelling) as a row-layer-only boundary helper, documented for the indexed-nullable-reference-column case specifically, never presented as something a domain type itself should use. Full writeup: [`2. design/gorm-shared-infrastructure.md`](../2.%20design/gorm-shared-infrastructure.md)'s "What should stay put, or wait" and "Sequencing" sections, both updated in this pass. |
| S14 | Extract `gormutil/`: `ClassifyError` (from comm's `classify`), dialect-aware constraint-sync helper, `SetUUIDIfEmpty` | 2026-09-15 | New [`gormutil/`](../../gormutil) package (one file + one test file, matching `httpwire`/`gormtest`/`vocab`'s own shape): `ClassifyError(err, wrapReference, wrapConflict)` generalizes comm's `classify` — same SQLSTATE-vs-sqlite-text lookup mechanics, but takes the caller's own field-wrapping functions instead of hardcoding comm's `ErrInvalidReference`/`ErrConflict`, the same "share the mechanism, not the mapping" shape `httpwire.StatusFor` already uses for HTTP codes. `SyncPartialUniqueIndex(db, table, suffix, columns, where)` generalizes actor's `syncDefaultAnchorIndex`/`syncUniqueAttributeIndexes` and assetmanager's `syncSerialTagUniqueIndex` — the `CREATE UNIQUE INDEX IF NOT EXISTS <table>_<suffix>_uniq ON <table> <columns> WHERE <where>` templating, dialect gate (postgres/sqlite only), and error wrapping every existing caller already retyped identically; the per-dialect JSON-path-vs-plain-column expression building stays the caller's own job, per the design doc's own note that this is "a small enough seam to parameterize" rather than generalize further. Deliberately did **not** attempt forms' `syncConstraints` — that's a postgres-only trigger+CHECK+index batch, not a partial-unique-index shape, and the design doc's own item 8 only named the index-sync scaffolding as shared, not that whole function. `SetUUIDIfEmpty(id *string)` generalizes the byte-identical `if r.ID == "" { r.ID = uuid.NewString() }` `BeforeCreate` body from actor/forms/git's row structs (comm mints sequence-numbered ids instead via its own `mintID`, out of scope). **No consumer repo touched** — per the design doc's own sequencing note ("additive... no repo is forced to adopt them until it wants to"), matching S12/S13/S15's identical precedent; adoption is opportunistic, same as those three. Verified: `go build ./... && go vet ./... && go test ./...` all green (13 new tests in `gormutil`, covering nil/not-found/unmapped passthrough, both dialects' constraint classification including the Postgres constraint→column→table name fallback chain, partial-index enforcement plus idempotency across a repeated migrate, and UUID-minting-only-when-empty). One process note, worth being honest about: forgot to flip this row's Status to 🚀 before starting (this skill's own convention) — caught and corrected only once the implementation was already done; re-checked `git status`/`git log` at that point and confirmed no concurrent session had touched this repo in the meantime, so no actual collision resulted this time. |

## Archived board context

`mwanachama-backend-shared` — Go library, module `github.com/aosanya/mwanachama-backend-shared`.
No audience of its own; a dependency of [mwanachama-backend-git](../../../mwanachama-backend-git)
and [mwanachama-backend-taskmanager](../../../mwanachama-backend-taskmanager), which are in turn
wired into [mwanachama-backend-api-gateway](../../../mwanachama-backend-api-gateway) for
[mwanachama-frontend-kazi](../../../mwanachama-frontend-kazi).

### Why this repo exists

`CodeValdGit` and `CodeValdWork` are ArangoDB-backed Go libraries built for
CodeValdCortex agencies. `mwanachama-frontend-kazi` needs the same git-like versioned
content and task/workflow management, but `mwanachama-backend-api-gateway` standardizes
on Postgres and runs as a single service (no sub-services). Both source
libraries lean on a shared package, `CodeValdSharedLib/entitygraph` — a
generic entity/relationship graph store — for almost everything that touches
storage; their own business logic (`git_impl_*.go`, `task*.go`) is already
storage-agnostic Go that only calls the `entitygraph.DataManager` interface.

Porting that one shared engine to Postgres once, here, means
`mwanachama-backend-git` and `mwanachama-backend-taskmanager` can port their respective
business logic with minimal churn instead of each reinventing entity/edge
storage and graph traversal.

Full task breakdown and rationale: see the plan this was scoped from
(`/Users/tony/.claude/plans/kind-snacking-rose.md` at the time of writing —
noted here since it predates any file in this repo).
