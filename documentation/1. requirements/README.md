# Requirements

Problem, vision and scope for `mwanachama-backend-shared`. Empty at bootstrap —
populate alongside S2 (the entity-graph contract) once the exact scope of
what's ported from `CodeValdSharedLib/entitygraph` vs. redesigned is settled.
